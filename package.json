{
  "name": "multitenant-offlinefirst-deskbooking",
  "private": true,
  "version": "1.0.0",
  "description": "Multi-Tenant Offline-First Desk & Cubicle Booking SaaS Platform",
  "scripts": {
    "dev": "pnpm --parallel dev",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter shared build && pnpm --filter api build && pnpm --filter web build",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:generate": "pnpm --filter api run prisma:generate",
    "db:push": "pnpm --filter api run prisma:push",
    "db:seed": "pnpm --filter api run prisma:seed"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "bcryptjs"
    ]
  }
}
